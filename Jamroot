import os ;
local HOME = [ os.environ HOME ] ;

use-project /libea : ../ealib/libea ;

exe evocadx-centroid :
    src/centroid.cpp
    /libea//libea_runner
    /boost//filesystem
    /boost//system
    : <link>static ;

install dist : 
    evocadx-centroid
    : <location>$(HOME)/bin ;
